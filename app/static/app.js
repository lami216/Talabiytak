document.addEventListener('submit',async e=>{const f=e.target;if(f.dataset.confirm&&!confirm(f.dataset.confirm)){e.preventDefault();return}if(f.classList.contains('upload-form')){f.querySelector('button').disabled=true;f.querySelector('.loading').hidden=false}if(f.classList.contains('save-product')){e.preventDefault();const b=f.querySelector('button');b.disabled=true;try{const r=await fetch(f.action,{method:'POST',body:new FormData(f),headers:{'X-Requested-With':'fetch'}}),d=await r.json();if(!r.ok)throw Error(d.message);f.closest('.image-card').classList.add('saved');f.innerHTML='<p class="success">تم حفظ المنتج</p>';const next=document.querySelector('.save-product input[name=name]');if(next)next.focus()}catch(x){alert(x.message);b.disabled=false}}});
